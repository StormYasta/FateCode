# Tema claro

O FateCode agora possui `ThemeProvider` com modos `dark` e `light`.

A preferência é salva em `localStorage` como `fatecode_theme`; quando ainda não existe preferência, a aplicação usa `prefers-color-scheme` do sistema operacional.

O botão de alternância fica na Navbar.
